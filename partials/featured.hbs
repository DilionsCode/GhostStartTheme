<div class="container-fluid d-flex justify-content-center section">
  {{#foreach posts limit="3" featured="true"}}
  <div class="col-md-4 featured-post" style="background: url({{feature_image}});">
    <h1>{{title}}</h1>
  </div>
  {{/foreach}}
</div>
